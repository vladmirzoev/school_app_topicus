package somtoday.registrations.sql;

/**
 * Creates a registration depending on parent inputs and is a Create function of the CRUD operations.
 * This class will take the inputs from the parent and perform an SQL insert into the database.
 *
 * A check will be run to see if such a registration entry exists.
 */
public class CreateRegistrationSQL {
}
