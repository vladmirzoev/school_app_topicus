package somtoday.registrations.sql;

/**
 * Fetches a registration from the database using SQL and fills up a template using the values of the entry's
 * attributes.
 *
 * A check will be run if the registration that will be exported is indeed the registration queried using SQL.
 */
public class ExportRegistrationSQL {
}
